/*
 * GRID.hpp
 *
 *  Created on: Nov 30, 2025
 *      Author: lucasnascimento
 */

#ifndef INC_GRID_GRID_HPP_
#define INC_GRID_GRID_HPP_

#include <cstdint>
#include <vector>
#include "main.h"

#define PANEL_W 48
#define PANEL_H 48

enum panel {
	panel_1_opcode = 0x00,
	panel_2_opcode = 0x01,
	panel_3_opcode = 0x02,
	panel4_opcode= 0x03
};

class GRID {
	private:
		uint8_t width = 0;
		uint8_t height = 0;
		uint8_t n_panels = 0;

		std::vector<std::vector<uint8_t>> panel_1;
		std::vector<std::vector<uint8_t>> panel_2;
		std::vector<std::vector<uint8_t>> panel_3;
		std::vector<std::vector<uint8_t>> panel_4;
		std::vector<std::vector<uint8_t>> panel;
		uint8_t busDetect();
		uint8_t busSelect(uint8_t i, uint8_t j);

		uint8_t panelSelect(uint8_t i, uint8_t j);

	public:
		explicit GRID();
		void bufferRead();
		void bufferWrite(uint8_t i, uint8_t j);
		void displayGrid();


};




#endif /* INC_GRID_GRID_HPP_ */
